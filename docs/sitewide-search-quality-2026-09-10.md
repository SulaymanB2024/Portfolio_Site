# Portfolio search-quality repair — September 10, 2026

PR #34 changed only the verification workflow. This change repairs the missing source work against main `4361ffa86f655433811efa4772a597acc5cb6706` (tree `04517d25a262bcd509870fbd047025414cd05eb1`). It does not merge unrelated PRs or alter analytics permissions.

## Scope and actual defects

The baseline build contains 161 HTML delivery files, 81 canonical pages, 78 sitemap entries, 26 article records, and 36 diagnostic guides. A new parser-based scan found **37 distinct page-level broken references**: 36 nonexistent foundation links in the diagnostic library and the missing rare-earth methodology download. Flat/directory variants repeated those failures; there were not 74 independent editorial defects.

All 36 foundation references now point to six existing research articles. Their related guides are selected by shared foundation and primary documentation rather than neighboring array positions. Each foundation article links back to its diagnostic applications through the shared article-search-target function used by both React and static rendering. Existing link-depth and contextual inbound-link requirements remain in place.

The guide builder no longer appends uppercase slug markers or pads prose to evade a repeated-phrase detector. Each diagnostic retains its own signal, mechanism, consequence, false-positive boundary, repair, and acceptance condition. Shared safety instructions may repeat. The revised gate tests distinct substantive decisions, valid destinations, reciprocal links, bounded length, source records, and copyable commands instead of requiring every 16-word passage to differ. Fixtures remain labeled as fixtures, not client results. Deliberately excluded URLs are not indiscriminately made indexable.

## Content and profile corrections

- The AI managers title exposes the business question and **30 mixed cases**, without presenting simulations, pilots, or exclusions as autonomous businesses.
- The Austin map states that its 67 records include partial and unresolved ownership. The July evidence cutoff is retained.
- The rare-earth article, maturity SVG, scenario labels, and CSV distinguish the **37,750 mixed stated-unit total** from **37,584–38,048 normalized metric tonnes**. The missing methodology companion is restored from the existing model and August 17 cutoff, not a new plant survey. Model numbers are unchanged; CSV units are explicit.
- The robots article opens with a two-source official-documentation table and links to RFC 9309 section 3. RFC 9309 and Google's robots introduction were reread for this correction; other historical source-verification dates were not advanced.
- The shared profile uses **BBA Finance / May 2028** and **Growth & Product Intern, Chegg / May 2026–Present**, as stated in the owner's September 1 canonical résumé. Obsolete Music-degree claims are removed from the homepage/About/résumé/`llms.txt` surfaces. Music remains a background interest. Sapien work is described without an invented formal title or start date.
- The public résumé PDF is generated from the shared facts, visually checked, and bound to a profile/PDF hash manifest. Confidential-company anonymity and first-party revenue qualifications are preserved. Private source documents are not published.

## Implementation and regression checks

Parent navigation now handles nested paths, trailing slashes and query strings without confusing sibling prefixes; markets research activates Research. Smooth scrolling observes live reduced-motion and native-device changes, cancels its animation loop, and destroys only its owned instance. Static generation removes its previous fallback before rerunning, avoiding duplicated sections, IDs and H1s.

The new standard-library HTML parser checks every generated local link, fragment, asset, canonical, sitemap URL, noindex exclusion, title/description/H1, ID, image alt attribute and JSON-LD block. It compares flat and directory delivery variants byte-for-byte. It permits the existing explicitly noindex software-buyout supporting report without treating it as an indexable canonical page. Twelve synthetic tests cover valid and intentionally broken fixtures. Nine behavior/content tests cover navigation, scroll cleanup, claim boundaries and reciprocal links.

The repaired local build checked **5,647 local references with zero errors**. All 78 indexable routes remain reachable within two hops under the existing verifier's graph definition; all 26 articles and 36 guides retain at least three contextual inbound sources. Repeated generation is byte-identical across 161 HTML files. CI reruns the exact submitted tree; the final PR records its run and merge identities rather than relying on the earlier baseline pass.

The optional browser smoke script exercises ten changed/important routes at 1440px and 390px, checks runtime errors and horizontal overflow, and changes motion/viewport preferences live. It runs on CI's Chrome because the authoring container blocks browser loopback requests. External requests are aborted in this test, so it does not certify third-party fonts, analytics, or production hosting.

## Boundaries

Existing permanent redirects were already correct and are preserved. The toll-road winner's URL and article proposition are unchanged. Source-level and built-site checks do not constitute a new verification of every external citation, acquisition or employer record, or prove search-performance improvements. Later Search Console evaluation needs comparable completed page/query/device/country periods, not a claim that an aggregate CTR change was caused by a title edit.
